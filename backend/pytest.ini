[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = tests.py test_*.py *_tests.py
markers =
    slow: long-running tests (can be skipped in quick runs)
    smoke: quick endpoint smoke tests
