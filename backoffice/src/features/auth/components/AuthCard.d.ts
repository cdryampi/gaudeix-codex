interface AuthCardProps {
    title: string;
    subtitle?: string;
    footer?: React.ReactNode;
    children: React.ReactNode;
}
export declare const AuthCard: ({ title, subtitle, footer, children, }: AuthCardProps) => import("react/jsx-runtime").JSX.Element;
export {};
