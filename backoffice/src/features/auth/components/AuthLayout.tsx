/**
 * AuthLayout re-export for feature-local imports.
 *
 * Some tests/components import the auth layout from the auth feature folder.
 * Keep this as a thin wrapper over the shared layout.
 */

export { AuthLayout } from "@/layouts/auth/AuthLayout";
